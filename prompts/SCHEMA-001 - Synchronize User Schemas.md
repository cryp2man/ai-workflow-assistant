
ВАЖНО:
Работаем строго по принципу Surgical.
Меняем только файлы схем.
Не менять Repository, Service, API, Alembic, Models, Database, Docker.

========================================
Цель
========================================

После DB-004 поле telegram_id стало обязательным.

Сейчас UserCreate его не содержит, поэтому POST /users всегда падает.

Нужно синхронизировать Pydantic-схемы с моделью БД.

========================================
Изменить только:

src/schemas/user.py

========================================

Нужно:

1. Добавить обязательное поле telegram_id: int в class UserCreate

2. Добавить telegram_id: int в class UserResponse

3. Все остальные поля оставить без изменений.

4. Не менять названия классов.

5. Не менять конфигурацию моделей.

6. Не менять API.

7. Не менять SQLAlchemy модели.

========================================
После изменений проверить:

1. import src.main должен проходить без ошибок.

2. Swagger должен запускаться.

3. POST /users теперь должен принимать JSON вида

{
    "username": "test",
    "email": "test@test.com",
    "telegram_id": 123456789
}

4. Ruff
uv run ruff check src/schemas/user.py

========================================
В отчёте указать

1.
Что изменено.

2.
Какие поля добавлены.

3.
Изменилось ли API.

4.
Проходит ли импорт.

5.
Проходит ли Ruff.

Никаких дополнительных улучшений не делать.