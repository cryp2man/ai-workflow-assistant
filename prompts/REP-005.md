REP-005 - добавить методы для WorkflowStepRepository

Контекст проекта

В проекте уже существуют:

- Workflow
- WorkflowStep
- WorkflowRun
- User

Репозитории:

- UserRepository
- WorkflowRepository
- WorkflowRunRepository

Теперь необходимо создать репозиторий для WorkflowStep.

========================================

Работаем строго по принципу Surgical.

Разрешается изменить только

src/repositories/workflow_step_repository.py

src/repositories/__init__.py

========================================

Не изменять

Models

Services

API

Schemas

Engine

Alembic

Tests

========================================

Создать

WorkflowStepRepository(BaseRepository)

========================================

Реализовать методы

create(step: WorkflowStep) -> WorkflowStep

get_by_id(step_id: int) -> WorkflowStep | None

list() -> list[WorkflowStep]

list_by_workflow(workflow_id: int) -> list[WorkflowStep]

========================================

ВАЖНО

list_by_workflow()

обязательно должен возвращать шаги

в порядке

step_order ASC

Использовать

order_by(WorkflowStep.step_order)

========================================

delete(step: WorkflowStep) -> None

========================================

Стиль полностью повторяет

WorkflowRepository

WorkflowRunRepository

========================================

commit()

rollback()

refresh()

НЕ использовать

flush()

использовать только

create()

delete()

========================================

После выполнения проверить

import src.main

uv run ruff check src/repositories

========================================

В отчёте указать

1. Какие методы реализованы.

2. Почему list_by_workflow() сортирует по step_order.

3. Используется ли commit.

4. Используется ли flush.

5. Проходит ли импорт.

6. Результат Ruff.

Если потребуется менять другие файлы - остановиться и сообщить об этом.

Никаких дополнительных улучшений не выполнять.