SER-004 - WorkflowRunService

Контекст проекта

FastAPI
SQLAlchemy 2.x
Service Layer

В проекте уже существуют:

- UserService
- WorkflowService

И только что реализован:

- WorkflowRunRepository

Необходимо создать сервис для WorkflowRun.

========================================

Работаем строго по принципу Surgical.

Разрешается изменить только

src/services/workflow_run_service.py

src/services/__init__.py

Если файла workflow_run_service.py нет - создать его.

========================================

Создать класс

WorkflowRunService

========================================

Конструктор

должен принимать

WorkflowRunRepository

и сохранять его как

self.repository

========================================

Реализовать методы

create_workflow_run(
    workflow_run: WorkflowRun,
) -> WorkflowRun

get_workflow_run(
    run_id: int,
) -> WorkflowRun | None

list_workflow_runs(
) -> list[WorkflowRun]

list_workflow_runs_by_workflow(
    workflow_id: int,
) -> list[WorkflowRun]

delete_workflow_run(
    workflow_run: WorkflowRun,
) -> None

========================================

Поведение должно полностью соответствовать

UserService

WorkflowService

========================================

Для методов

create_workflow_run()

delete_workflow_run()

использовать

repository.*

+

commit()

+

rollback()

при IntegrityError

========================================

Для read-only методов

никаких commit()

никаких rollback()

========================================

Никаких новых исключений.

После rollback повторно выбрасывать исходное исключение.

========================================

После выполнения проверить

import src.main

uv run ruff check src/services/workflow_run_service.py

========================================

В отчёте указать

1. Какие методы реализованы.

2. Где используется commit.

3. Где используется rollback.

4. Почему commit находится в Service.

5. Какие исключения обрабатываются.

6. Проходит ли импорт.

7. Результат Ruff.

Никаких дополнительных улучшений не выполнять.