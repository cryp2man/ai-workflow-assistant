
Работай строго по правилам проекта.

Нельзя делать ничего сверх задачи.

Нельзя рефакторить.

Нельзя улучшать код.

Нельзя менять архитектуру.

Нельзя менять другие файлы.

Если потребуется изменение других файлов - сначала остановись и спроси меня.

----------------------------------

Задача: ENGINE-008

Файл:
- src/engine/execution_engine.py

Необходимо реализовать обработку ошибок выполнения Workflow.

Изменения:

1. Весь цикл выполнения шагов должен быть обёрнут в

try:
    ...

except Exception as e:

2. Если произошла ошибка:

WorkflowRun должен получить

status = "failed"

finished_at = datetime.utcnow()

error = str(e)

3. После изменения объекта необходимо выполнить

await self.workflow_run_service.repository.session.commit()

(использовать существующую архитектуру проекта)

4. После commit необходимо повторно выбросить исключение

raise

никаких новых исключений создавать нельзя.

5. Если ошибок не произошло - поведение completed должно остаться полностью без изменений.

То есть:

running

↓

выполнение шагов

↓

completed

↓

commit

↓

return WorkflowRun

6. Не менять:

- PromptBuilder
- Repository
- Services
- Models
- API
- LLM Provider
- WorkflowStep
- WorkflowRun
- Workflow

7. После выполнения предоставить отчёт:

8. Какие строки изменены.

9. Как теперь выглядит жизненный цикл WorkflowRun.

10. Почему commit выполняется и в completed, и в failed.

11. Почему используется raise без создания нового исключения.

12. Проходит ли import src.main.

13. Результат

uv run ruff check src/engine

Никаких дополнительных улучшений не делать.