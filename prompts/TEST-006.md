TEST-006 - Test Database Migrations.

ВАЖНО.

Работаем строго по принципу Surgical.

========================================

НЕ менять

- src/
- API
- Repository
- Service
- Models
- Production Database
- compose.yaml

========================================

Разрешается изменить только

tests/

alembic/

если это необходимо исключительно для тестовой инфраструктуры.

========================================

Цель

Подготовить test_db к выполнению тестов с использованием Alembic.

Использовать только существующие миграции проекта.

========================================

Необходимо

1. Не использовать Base.metadata.create_all()

2. Не использовать drop_all()

3. Не создавать таблицы вручную.

4. Применить существующие Alembic migration к test_db. Если требуется отдельная конфигурация Alembic для тестовой базы - создать её.

Production БД использовать запрещено.

========================================

После применения миграций проверить что таблицы users workflows существуют.

========================================

После этого запустить uv run pytest

========================================

Если после применения миграций тесты падают уже НЕ из-за отсутствия таблиц - это считается корректным результатом.

========================================

Никаких изменений Repository,

Service,

API,

Models

не делать.

========================================

В отчёте указать

1. Какие миграции были применены.

2. Какие таблицы появились.

3. Удалось ли пройти дальше ошибки UndefinedTable.

4. На каком этапе теперь останавливаются тесты.

Никаких дополнительных улучшений не делать.